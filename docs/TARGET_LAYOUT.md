# Target layout in Word and LibreOffice

## Shared rule (all platforms)

**Canonical text** uses Unicode Kanbun from marinaMoji (`說㆒㆑者`).  
**Presentation** is generated by a **manual** host command (**Format kaeriten**). Never treat presentation alone as the archival format.

| Layer | LibreOffice | Word | OnlyOffice (v1) |
|-------|-------------|------|-----------------|
| Source | `說㆒㆑者` | `說㆒㆑者` | `說㆒㆑者` |
| Primary view | Anchored **frame** | Anchored **textbox** | None (use source or subscript fallback) |
| Export input | Source + `mapping.json` | Source + `mapping.json` | Source + `mapping.json` |

See [ARCHITECTURE.md](ARCHITECTURE.md), [LIBREOFFICE_FRAMES.md](LIBREOFFICE_FRAMES.md), [WORD_FINDINGS.md](WORD_FINDINGS.md).

## User workflow (both LO and Word)

```text
1. Type source:     說㆒㆑者
2. Format kaeriten:  (selection / paragraph / document)
3. Edit source only — not inside frame/textbox
4. Refresh rendering after mark or style changes
```

Auto-format while typing is **out of scope** for v1.

## LibreOffice Writer — primary (frame-based)

### Professional renderer: anchored frame

Used for **simple and compound** kaeriten.

- Borderless frame, **anchor as character** after the annotated kanji.
- Stacked glyphs inside frame (e.g. `㆒㆑` → 一 over レ).
- Bottom-aligned content, ~0.2 mm offset, small font (see frame doc for parameters).
- Works in **vertical text**.

Triggered by **Format kaeriten**, not ruby.

### Fallback: character styling

- Unicode ㆑ or subscript レ, reduced size.
- Use when extension unavailable or for trivial single レ only.
- **Insufficient** for compound kaeriten (一レ, 上レ, etc.).

### Rejected: Asian ruby

- Phonetic guide API places text like **furigana**, not kaeriten.
- Cannot naturally stack compound marks.

## Microsoft Word — Phase 3 (textbox-based)

Same canonical source; **textbox** renderer (validated May–June 2026). No LibreOffice frame import.

### Primary: anchored textbox

- Same role as LO frames: borderless, as-character anchor, stacked glyphs.
- Better positioning flexibility in some cases; same font-scale and search limitations.
- Users edit `說㆒㆑者`, not textbox content.

### Fallback: subscript / EQ

- Subscript レ / 一 / 二 per [mapping.json](../mapping.json).
- EQ fields for okurigana + レ — see [SOURCES.md](SOURCES.md).

### Rejected: Phonetic Guide (ruby)

- Same rejection as LibreOffice.

## OnlyOffice

- **Plugin (v0.1):** inline content controls via `Api.CreateInlineLvlSdt()` — see [onlyoffice/README.md](../onlyoffice/README.md).
- Paste from LO **loses frame objects** — re-render from Unicode source in ONLYOFFICE.
- Rely on **visible Unicode source** in the document for interchange.
- **Copy as plain text** / TEI / LaTeX from the plugin sidebar.
- Do not depend on hidden metadata shared with LO or Word.

## Okurigana (both platforms, later)

**送り仮名** → ruby (ルビ), katakana in dialog — separate from kaeriten renderer.

## 訓点マクロ (historical contrast)

Tag → batch convert to ruby/subscript. marinaMoji skips tags; source is already `㆒㆑`.  
篠原 (2024): direct subscript/ruby is simpler for basic marks; tags mainly help 再読 in Word.

## 漢文エディタ import (optional)

`登る[レ]山に。` ↔ `登ル^山ニ。` — importer to Unicode source, then same **Format kaeriten** pipeline.

## TeX (export from source, not from frames)

`kunten2e.sty`, `sfkanbun-lua.sty` — [SOURCES.md](SOURCES.md). Exporters read canonical `說㆒㆑者`, not LO frames or Word shapes.
